# dependencies
node_modules/

# builds
dist/
build/
*.tsbuildinfo

# the desktop app's packaged output (the .dmg and the unpacked .app)
release/

# runtime data (workspaces, sqlite db, logs)
workspaces/
data/

# scratch space for the e2e run (its own sqlite db + workspaces)
.e2e/
test-results/
playwright-report/
coverage/
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm

# config secrets (keep the example tracked; real keys stay local)
config/config.local.yaml

# env
.env
.env.local
.env.*.local

# editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.log

# Claude Code — per-machine state, not the shared parts. `.claude/skills/` and
# `skills-lock.json` are shared (committed); the permission grants below are whatever this
# clone happened to approve, and worktrees are scratch.
.claude/settings.local.json
.claude/worktrees/